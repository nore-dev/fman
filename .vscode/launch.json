{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "fman:debug",
      "type": "go",
      "request": "launch",
      "mode": "exec",
      "program": "${workspaceFolder}/build/bin/fman",
      "preLaunchTask": "build debug",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
